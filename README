Application Details
Company:

110A_team

Application Name:

resume

API Key:

f912s66vnvet

Secret Key:

h2bqZEQUd5qS8zRf



nodejs + express + jade 

Global setup:

 Set up git
  git config --global user.name "Your Name"
  git config --global user.email filmuzhik@gmail.com
      

Next steps:

  mkdir uwc-final
  cd uwc-final
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:wtfil/uwc-final.git
  git push -u origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:wtfil/uwc-final.git
  git push -u origin master
      

Importing a Subversion Repo?

  Check out the guide for step by step instructions.
      

When you're done:

  Continue


